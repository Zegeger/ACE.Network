using System;

namespace ACE.Network.Enums
{
    /// <summary>
    /// Actions related to /allegiance house
    /// </summary>
    public enum StringInfoType : byte
    {
        FileSource = 0x00,
        Embedded = 0x01
    }
}
